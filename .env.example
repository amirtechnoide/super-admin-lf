# --- Option A : le backend n'expose pas de CORS (cas actuel) ---------------
# Next relaie les appels serveur à serveur : le navigateur reste same-origin.
API_PROXY_TARGET=https://sogafric-backend.lf-company.com
NEXT_PUBLIC_API_BASE_URL=/api/backend

# --- Option B : le backend autorise l'origine du dashboard ------------------
# Appel direct, sans proxy. Laisser API_PROXY_TARGET vide.
# NEXT_PUBLIC_API_BASE_URL=https://sogafric-backend.lf-company.com
